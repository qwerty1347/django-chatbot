services:
  app:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    container_name: django_chatbot
    volumes:
      - ./:/app
    ports:
      - "9097:8000"
    networks:
      - local_network

networks:
  local_network:
    driver: bridge
